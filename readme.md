
5. Visit `http://127.0.0.1:8501/` in your web browser


Clone this repo
Make .env in the root of the project
Run the project...
for running in the root of the project.

docker compose build
docker compose up -d

Deploy it and provide the ip address of it .
Then this is integrated in the growise application you just have to provide the ip address 